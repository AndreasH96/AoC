import numpy as np 
import sys
sys.path.insert(0,'../..')
from HelperFunctions.helperfunctions import readInputAsNumpy

directions = [[1,0],[0,1],[-1,0],[0,-1]]
def readInput(path):
    return np.array([[int(i) for i in row] for row in open(path, "r").read().split('\n')])
def isLocalMinimum(position,matrix):
    n.any(column_name < 0)

test = readInput('testinput.txt')

np.any


